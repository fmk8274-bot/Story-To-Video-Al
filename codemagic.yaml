workflows:
  android-build:
    name: Build Android APK
    instance_type: mac_mini_m1
    max_build_duration: 60
    scripts:
      - name: Extract project
        script: |
          unzip story-to-video-ai.zip
    artifacts:
      - "*.apk"

